const list=document.querySelector('#project-list'),modal=document.querySelector('#project-modal'),modalContent=document.querySelector('#modal-content');document.title=`${portfolio.owner.name} | 作品集`;document.querySelector('.brand').innerHTML=`<span>${portfolio.owner.en}</span><i>.</i>`;document.querySelector('#email-link').href=`mailto:${portfolio.owner.email}`;document.querySelector('#email-link').innerHTML=`${portfolio.owner.email} <span>↗</span>`;document.querySelector('#footer-name').textContent=`© ${portfolio.owner.name}`;
list.innerHTML=portfolio.projects.map((p,i)=>`<article class="project" data-project="${i}" tabindex="0" role="button" aria-label="打开 ${p.title} 项目"><span class="project-number">0${i+1}</span><div class="project-image" style="background-image:url('${p.cover}')"></div><div class="project-info"><h3>${p.title}</h3><p>${p.subtitle}</p></div><div class="project-tags">${p.tags.map(t=>`<span>${t}</span>`).join('')}</div><span class="project-arrow">↗</span></article>`).join('');
function openProject(i){const p=portfolio.projects[i];document.querySelector('#modal-count').textContent=`0${i+1} / 0${portfolio.projects.length}`;modalContent.innerHTML=`<section class="modal-intro"><h2>${p.title}</h2><p><strong>${p.subtitle}</strong><br /><br />${p.description}</p></section><div class="modal-gallery">${p.pages.map(image=>`<figure><img src="${image}" alt="${p.title} 作品展示" loading="lazy" /></figure>`).join('')}</div>`;modal.showModal();modal.scrollTop=0}document.querySelectorAll('.project').forEach(x=>{x.addEventListener('click',()=>openProject(+x.dataset.project));x.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();openProject(+x.dataset.project)}})});document.querySelector('.close-modal').addEventListener('click',()=>modal.close());modal.addEventListener('click',e=>{if(e.target===modal)modal.close()});document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener('click',e=>{const target=document.querySelector(link.getAttribute('href'));if(target){e.preventDefault();target.scrollIntoView({behavior:'smooth'})}}));
